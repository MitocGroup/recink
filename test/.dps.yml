--- 
$:
  # cache:
  #   driver: 's3'
  #   options:
  #     accessKey: 'process.env.DPS_AWS_ACCESS_KEY'
  #     secretKey: 'process.env.DPS_AWS_SECRET_KEY'
  #     path: 'process.env.DPS_S3_CACHE_PATH'
  npm:
    installPackageDependencies: true
  emit:
    pattern:
      - /.+\.js$/i
    ignore:
      - /^(.*\/)?node_modules(\/?$)?/i
  test:
    mocha:
      options:
        - { ui: 'bdd' }
    pattern:
      - /.+\.spec\.js$/i
  # coverage:
  #   ignore:
  #     - /.+\.spec\.js$/i
main:
  root: '.'
  dependencies:
    chai: '^3.5.0'
    # local-sample: 'file:./sample-module'
